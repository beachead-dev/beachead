// beachead-askpass: GIT_ASKPASS credential helper binary.
// Reads credentials from the OS keyring and prints them to stdout for git.
// Full implementation in task 3.2.

fn main() {
    // Stub — will be implemented in task 3.2
    eprintln!("beachead-askpass: not yet implemented");
    std::process::exit(1);
}
